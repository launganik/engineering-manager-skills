{
  "mcpServers": {
    "github": {
      "command": "gh",
      "args": ["copilot", "mcp"],
      "description": "GitHub signals: PR frequency, review participation, commit activity, review lag"
    },
    "atlassian": {
      "command": "npx",
      "args": ["-y", "@anthropic/claude-atlassian-mcp"],
      "description": "Jira + Confluence signals: ticket velocity, blocked tickets, pages authored, comments"
    },
    "slack": {
      "command": "npx",
      "args": ["-y", "@anthropic/claude-slack-mcp"],
      "description": "Slack signals: channel participation trend, response latency (public channels only)"
    },
    "google-calendar": {
      "command": "npx",
      "args": ["-y", "@anthropic/claude-google-calendar-mcp"],
      "description": "Calendar signals: meeting load %, focus time blocks, 1:1 adherence"
    },
    "zapier": {
      "type": "http",
      "url": "https://mcp.zapier.com/api/v1/connect"
    }
  }
}
