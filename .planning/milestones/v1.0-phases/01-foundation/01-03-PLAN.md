---
phase: 01-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - SKILL.md
autonomous: true
requirements:
  - REF-04

must_haves:
  truths:
    - "An EM who has never used this skill can read SKILL.md and complete installation with no other reference"
    - "SKILL.md lists all MCP prerequisites with links and explains what happens when each is absent"
    - "Every slash command is described with its purpose, required arguments, and an example invocation"
    - "First-run steps are numbered and lead directly to a working /team-health:setup invocation"
  artifacts:
    - path: "SKILL.md"
      provides: "Human-facing installation guide and command reference for the team-health skill"
      min_lines: 80
  key_links:
    - from: "SKILL.md"
      to: "/team-health:setup"
      via: "First-run steps tell the EM to run /team-health:setup after installing skill files"
      pattern: "/team-health:setup"
---

<objective>
Create the top-level SKILL.md that serves as the human-facing installation guide. This is what an EM reads when they discover this skill - it must be self-contained, accurate, and lead them from zero to a working first run without needing to look anywhere else.

Purpose: Without this guide, EMs cannot install the skill. It is the front door. It also documents all commands so the EM knows what they are getting before they commit to setup.

Output: SKILL.md at the project root.
</objective>

<execution_context>
@/Users/kamallaungani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kamallaungani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-foundation-01-SUMMARY.md
@.planning/phases/01-foundation/01-foundation-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create top-level SKILL.md</name>
  <files>SKILL.md</files>
  <action>
Create `SKILL.md` at the project root. This is a human-readable markdown document - not a Claude Code command file. It does not have YAML front matter for Claude Code invocation.

Write the file with these sections in order:

**Section 1: Header and Tagline**
```markdown
# team-health

Help for engineering managers who want to be more human - remembering what their people care about, noticing when someone might be struggling, keeping commitments.

This skill surfaces what a good manager with infinite attention span would already notice.
```

**Section 2: What This Does**

3-4 sentence description. Cover: what signals it tracks, what it does NOT do (diagnose, score, judge), who it is for (EMs, not automated pipelines), and that data stays local.

**Section 3: Prerequisites**

Subsection: Required
- Claude Code (code.claude.com) installed and active

Subsection: Optional MCP Sources
A table with four rows. Each row: MCP Source | What It Enables | How to Add | What's Missing Without It

| MCP Source | Signals Enabled | Install Command | Absent signals |
|---|---|---|---|
| GitHub MCP | PR frequency, review participation, commit activity, PR review lag | `claude mcp add github ...` | All PR/commit signals unavailable |
| Jira MCP | Ticket velocity, blocked tickets, sprint carry-over | `claude mcp add jira ...` | Jira signals unavailable |
| Slack MCP | Channel participation trend, response latency (metadata only, no DM content) | `claude mcp add slack ...` | Slack signals unavailable |
| Google Calendar MCP | Meeting load %, 1:1 adherence | `claude mcp add google-calendar ...` | Calendar signals unavailable |

Note: "The skill works without any MCP sources - you will just have fewer signals. GitHub is the most valuable source for most engineering teams."

**Section 4: Installation**

Numbered steps:

1. Copy the skill files into your project directory:
   ```bash
   # Option A: Clone this repo into your project
   git clone https://github.com/your-org/engineering-manager-skills .

   # Option B: Copy just the skill files
   cp -r /path/to/engineering-manager-skills/.claude ./
   cp /path/to/engineering-manager-skills/SKILL.md ./
   ```

2. Verify the command files are present:
   ```bash
   ls .claude/commands/team-health/
   # Should show: setup.md (and others as they are added)
   ```

3. Open Claude Code in your project directory and run first-time setup:
   ```
   /team-health:setup
   ```
   The setup flow will ask for your team roster and detect which MCP sources are available.

4. Verify setup completed:
   ```bash
   cat .team-health/config.json
   # Should show setup_complete: true and your team roster
   ```

5. Verify gitignore protection:
   ```bash
   grep ".team-health" .gitignore
   # Should show: .team-health/
   ```

**Section 5: Commands**

One subsection per command. For each: command name, one-sentence description, syntax, example, and what it requires.

Commands to document (in this order):

`/team-health:setup`
- Description: First-time setup. Collects team roster, 1:1 cadences, and detects available MCP sources. Must be run before any other command.
- Syntax: `/team-health:setup`
- Flags: `--reset` to reconfigure an existing setup
- Requires: Nothing (runs first)

`/team-health:log <name>`
- Description: Append a note to a direct report's people log. Structures free-form input into dated, categorized entries.
- Syntax: `/team-health:log alice` or `/team-health:log "Alice Chen"`
- Example note: "gave feedback on the auth PR, really clean API design"
- Requires: Setup complete. Creates people/<slug>.json on first use.
- Status: Phase 2 (not yet available)

`/team-health:pulse`
- Description: Weekly team health scan. Scores each person against their personal 8-week baseline, flags anomalies conservatively, and stores baselines for future runs.
- Syntax: `/team-health:pulse`
- Output: Summary table (green/yellow/red per person) + detail sections for flagged individuals
- Requires: Setup complete. GitHub MCP strongly recommended.
- Status: Phase 3 (not yet available)

`/team-health:prep <name>`
- Description: Generate a 2-minute prep sheet for an upcoming 1:1. Synthesizes live tool signals against personal baselines and surfaces standing items from the people log.
- Syntax: `/team-health:prep alice`
- Output: Status snapshot, signal flags, standing items, talking points, context reminders
- Requires: Setup complete. Pulse run at least once recommended.
- Status: Phase 4 (not yet available)

`/team-health:skip-level`
- Description: Generate an upward-facing team brief. Aggregates team-level themes - individual names do not appear without explicit opt-in.
- Syntax: `/team-health:skip-level`
- Flags: `--weeks 2`, `--quarter`, `--include-person <name>`
- Requires: Setup complete. Pulse history.
- Status: Phase 5 (not yet available)

`/team-health:retro-prep`
- Description: Generate a data-seeded sprint retro agenda. Discussion seeds are attributed to work items, never to individuals.
- Syntax: `/team-health:retro-prep`
- Requires: Setup complete. Jira MCP recommended.
- Status: Phase 5 (not yet available)

**Section 6: Where Data Lives**

```
.team-health/              # Gitignored - stays on your machine
  config.json              # Team roster, MCP sources, settings
  baselines.json           # 8-week rolling baselines per person per signal
  people/                  # One file per direct report
    alice-chen.json        # Longitudinal notes, commitments, career context
  pulse-history/           # Weekly pulse snapshots
    2026-W11.md

.claude/
  commands/team-health/    # Slash command files (safe to commit)
  team-health/             # Reference docs loaded by commands (safe to commit)
    SIGNALS.md
    BASELINES.md
    PRIVACY.md
```

**Section 7: Privacy Design**

4-5 sentences. Cover: data stays local (no external service), gitignore by default, Slack signals are metadata only (no DM content), output uses behavioral observations not diagnoses, skip-level aggregates themes not individuals.

**Section 8: Upgrading / Uninstalling**

Upgrading: Pull or copy new skill files over `.claude/commands/team-health/` and `.claude/team-health/`. Your `.team-health/` data is unaffected.

Uninstalling: `rm -rf .claude/commands/team-health/ .claude/team-health/`. Optionally delete `.team-health/` to remove all local data.

Write the full document with all sections above, using clear markdown headers and properly formatted code blocks. The document should be accurate to the files created in this phase - do not describe features from future phases as if they are currently available; use "Status: Phase N (not yet available)" for future commands.
  </action>
  <verify>
    <automated>ls /Users/kamallaungani/Developer/engineering-manager-skills/SKILL.md && wc -l /Users/kamallaungani/Developer/engineering-manager-skills/SKILL.md</automated>
  </verify>
  <done>
    SKILL.md exists at project root with:
    - All 8 sections present
    - Installation steps numbered and include a verify step (cat config.json, check gitignore)
    - All 6 commands documented with syntax and status
    - MCP prerequisites table with all 4 sources
    - Data structure diagram showing .team-health/ and .claude/ layout
    - File is at least 80 lines
  </done>
</task>

</tasks>

<verification>
- `ls SKILL.md` exits 0
- `grep "/team-health:setup" SKILL.md` returns at least 2 matches (installation step + command reference)
- `grep "schema_version\|.team-health/" SKILL.md` returns matches confirming data structure is documented
- `wc -l SKILL.md` returns >= 80
</verification>

<success_criteria>
- SKILL.md exists at project root
- An EM with no prior context can follow the installation section and reach a working /team-health:setup invocation
- All 6 commands are documented with status indicating which phases are available now vs. later
- Privacy design is addressed explicitly (local data, no DM content, behavioral observations)
- MCP prerequisites table is accurate and includes install commands
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-foundation-03-SUMMARY.md`
</output>
