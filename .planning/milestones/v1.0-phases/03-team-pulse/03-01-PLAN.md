---
phase: 03-team-pulse
plan: "01"
type: execute
wave: 2
depends_on:
  - "03-00"
files_modified:
  - .claude/commands/team-health/pulse.md
autonomous: true
requirements:
  - PULSE-01
  - PULSE-02
  - PULSE-03
  - PULSE-04
  - PULSE-05
  - PULSE-06
  - PULSE-07
  - PULSE-08
  - PULSE-09
  - PULSE-10

must_haves:
  truths:
    - "Manager can run /team-health:pulse and receive a dashboard with a per-person green/yellow/red summary table"
    - "Flagged individuals have detail sections with specific metric name, delta, stddev delta, and MCP source - unflagged persons have table row only"
    - "Flags fire only when 2+ signals align OR a single signal exceeds 2 standard deviations from the person's own baseline"
    - "After each run, baselines.json is updated with computed_from, computed_to, source, and schema_version provenance fields"
    - "A pulse-history/YYYY-WNN.md snapshot file is written after each run"
    - "Command degrades gracefully when MCP sources are absent, naming which signals are unavailable"
    - "The required disclaimer appears verbatim at the end of every pulse output"
    - "Running the command before setup is complete routes to the setup flow instead of erroring"
  artifacts:
    - path: ".claude/commands/team-health/pulse.md"
      provides: "/team-health:pulse command - full pulse scan pipeline"
      contains: "disable-model-invocation: true"
  key_links:
    - from: ".claude/commands/team-health/pulse.md"
      to: ".team-health/baselines.json"
      via: "Read tool reads existing baselines; Write tool writes full updated file in single atomic call after output generation (Phase E)"
    - from: ".claude/commands/team-health/pulse.md"
      to: ".team-health/pulse-history/<ISO-week>.md"
      via: "Write tool writes snapshot after baselines.json update; mkdir -p creates directory on first run"
    - from: ".claude/commands/team-health/pulse.md"
      to: ".claude/team-health/SIGNALS.md + BASELINES.md + PRIVACY.md"
      via: "Read tool loads all three reference docs before any analysis - signal thresholds, baseline algorithm, and output rules are applied verbatim from these docs"
    - from: ".claude/commands/team-health/pulse.md"
      to: ".team-health/config.json"
      via: "Pre-flight Check reads config.json; sources map gates each MCP section; team array drives the person loop"
---

<objective>
Write `.claude/commands/team-health/pulse.md` - the single command file that delivers all 10 PULSE requirements. This is Phase 3's primary deliverable. The command orchestrates the full weekly health scan: read reference docs, collect MCP signals per person, compare against personal baselines, apply the two-signal flagging rule, render the dashboard, and write two state files (baselines.json and pulse-history snapshot).

Purpose: Give managers a weekly team health scan that surfaces behavioral changes conservatively - only when the data is clear - with specific, attributable signals rather than opaque scores.
Output: .claude/commands/team-health/pulse.md - a complete Claude Code slash command file.
</objective>

<execution_context>
@/Users/kamallaungani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kamallaungani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/state-schemas.json
@.planning/phases/03-team-pulse/03-RESEARCH.md
@.claude/commands/team-health/setup.md
@.claude/commands/team-health/log.md
@docs/testing/phase-3-smoke-tests.md

<interfaces>
<!-- Key schemas and contracts the executor needs. Extracted from locked sources. -->
<!-- Use these directly - no codebase exploration needed. -->

From .planning/phases/01-foundation/state-schemas.json (locked):

config.json structure:
{
  "schema_version": "1",
  "setup_complete": true,
  "manager": { "name": "...", "github_username": "...", "timezone": "..." },
  "team": [
    {
      "name": "Alice Chen",
      "slug": "alice-chen",
      "github_username": "achen",
      "jira_user_id": "alice@company.com",
      "slack_user_id": "U0123ALICE"
    }
  ],
  "sources": { "github": true, "jira": false, "slack": false, "calendar": false },
  "baseline_window_weeks": 8,
  "github_org": "acme-corp"
}

baselines.json structure (locked - schema_version: "1"):
{
  "schema_version": "1",
  "last_updated": "2026-03-13",
  "people": {
    "alice-chen": {
      "computed_from": "2025-12-01",
      "computed_to": "2026-03-13",
      "source": ["github", "jira"],
      "window_weeks": 8,
      "metrics": {
        "github_prs_merged_per_week": {
          "window": [2, 3, 1, 2, 2, 3, 2, 2],
          "mean": 2.125,
          "stddev": 0.6,
          "last_value": 2
        }
      }
    }
  }
}

From .claude/commands/team-health/setup.md and log.md (Phase 1/2 reference implementations):
- Pre-flight Check pattern: Read config.json → check setup_complete → stop with message if false
- Allowed-tools front matter syntax: allowed-tools: Read, Write, Bash(date +%Y-%m-%d), Bash(date +%Y-W%V), Bash(mkdir -p .team-health/pulse-history)
- disable-model-invocation: true required on all state-writing commands
- Date injection: Current date: !`date +%Y-%m-%d` as first line of command body

Signal metric names (from SIGNALS.md - used as keys in baselines.json metrics object):
  GitHub (4 signals):
    github_prs_merged_per_week
    github_pr_review_count_per_week
    github_pr_review_lag_days
    github_commit_days_per_week
  Jira (3 signals):
    jira_tickets_closed_per_week
    jira_tickets_in_progress_over_2_sprints
    jira_blocked_tickets
  Slack (2 signals):
    slack_public_channel_participation_trend
    slack_response_latency_trend
  Calendar (2 signals):
    calendar_meeting_load_pct
    calendar_focus_time_blocks_per_week

Baseline update algorithm (from BASELINES.md - apply verbatim, do not re-derive):
  1. Append current week's value to window array
  2. If len(window) > baseline_window_weeks: remove first element (oldest)
  3. mean = sum(window) / len(window)
  4. stddev = sqrt(sum((x - mean)^2 for x in window) / len(window))
     Special case: if len(window) == 1, stddev = 0
  5. last_value = current week's value
  6. computed_to = today's date

First-run baseline behavior (from BASELINES.md - apply verbatim):
  - len(window) == 0 (person not in baselines.json): initialize window with current value; do NOT flag; show "(baseline pending - 0 weeks of data)"
  - len(window) == 1 or 2: do NOT flag; show "(baseline pending - N weeks of data)"
  - len(window) >= 3: compute deviations and apply flagging rules

Two-signal flagging rule (from SIGNALS.md Section 5 - apply verbatim):
  - YELLOW: 2+ signals each at >= 1 stddev below baseline (for frequency signals: lower = concerning)
  - RED: 1 signal at >= 2 stddev from baseline
  - GREEN: everything else
  NOTE: For lag signals (github_pr_review_lag_days), higher = concerning (flag on upward deviation)
        For all other signals, lower = concerning (flag on downward deviation)

Flag output format (PULSE-06 - use verbatim in command instructions):
  - **[metric_name_human_readable]:** [current_value] [unit] this week vs. 8-week avg [mean] [unit] ([delta:+/-N.N], [stddev_delta:+/-N.N] stddev)
    Source: [MCP source name] MCP

  Example:
  - **PR review count:** 0 reviews this week vs. 8-week avg 3.8/week (−3.8, −5.4 stddev)
    Source: GitHub MCP

Required disclaimer text (from PRIVACY.md - copy verbatim, no paraphrase allowed):
  "These signals are indicators, not diagnoses. Always talk to your people before drawing conclusions."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .claude/commands/team-health/pulse.md</name>
  <files>.claude/commands/team-health/pulse.md</files>
  <action>
Create `.claude/commands/team-health/pulse.md` with the complete command implementation. Build it section by section in the order below. Every section header and every piece of logic must appear verbatim as prompt instructions - this file IS the prompt.

**Front matter (copy exactly):**
```
---
description: "Weekly team health pulse - scans all direct reports, scores signals against personal baselines, and produces a flagged team dashboard."
argument-hint: ""
allowed-tools: Read, Write, Bash(date +%Y-%m-%d), Bash(date +%Y-W%V), Bash(mkdir -p .team-health/pulse-history)
disable-model-invocation: true
---

Current date: !`date +%Y-%m-%d`
Current ISO week: !`date +%Y-W%V`
```

**Section: Pre-flight Check (copy verbatim - mandatory boilerplate)**
```
## Pre-flight Check

Before doing anything else:
1. Read .team-health/config.json using the Read tool.
2. If the file does not exist OR if setup_complete is not true:
   - Tell the user: "Team Health is not set up yet. Run /team-health:setup to configure your team roster and MCP sources."
   - Stop. Do not proceed with the rest of this command.
3. If setup_complete is true, continue.
```

**Section: Required Reference Reads**
```
## Required Reference Reads

Before doing any analysis, read these files using the Read tool (in order):
1. .claude/team-health/SIGNALS.md - 11 signals, thresholds, two-signal rule, severity levels
2. .claude/team-health/BASELINES.md - baseline computation algorithm, first-run behavior, provenance fields
3. .claude/team-health/PRIVACY.md - output language rules, required disclaimer, degradation language

Follow these documents exactly. Do not improvise signal thresholds, flagging rules, or output language.
```

**Section: Phase A - Setup**
```
## Phase A - Setup

After reading reference docs:
1. Read .team-health/baselines.json using the Read tool.
   - If file does not exist or returns an error: treat as { "schema_version": "1", "last_updated": "", "people": {} }
   - Store the full structure in memory as CURRENT_BASELINES.
2. Read the sources map from config.json (already read in Pre-flight Check):
   - sources.github, sources.jira, sources.slack, sources.calendar
3. Note unavailable sources. For each source where the value is false, use PRIVACY.md Graceful Degradation Language to state which signals will be absent.
   Output at start of pulse run: "Sources active: [list true sources] | Unavailable: [list false sources] - [PRIVACY.md degradation phrasing]"
   If ALL sources are false: state all 4 are unavailable, list all 11 signals as absent, render the summary table with "no signals available" per person, include the disclaimer, then stop.
```

**Section: Phase B - Per-person signal collection**
```
## Phase B - Per-person signal collection

For each team member in config.json team array (iterate in order):
  Record their: name, slug, github_username, jira_user_id, slack_user_id

  For each available source (skip the entire source block if sources.<name> is false):

  ### GitHub signals (if sources.github is true):
  Scope ALL GitHub queries to the github_org field from config.json. Do not query across all of GitHub.
  Query the GitHub MCP for this person's github_username over the past 7 days (current week):
  - github_prs_merged_per_week: count of PRs merged by this person in github_org this week
  - github_pr_review_count_per_week: count of PR reviews submitted by this person in github_org this week
  - github_pr_review_lag_days: average days from PR creation to first review for PRs where this person is the author (within github_org, this week). Use 0 if no PRs awaiting review.
  - github_commit_days_per_week: count of distinct calendar days with at least one commit by this person in github_org this week

  Use whichever commit-listing and PR-listing tools are available from the GitHub MCP - probe by namespace (any tool containing 'github' or starting with 'mcp_github'). Adapt tool names to what the manager has installed.

  ### Jira signals (if sources.jira is true):
  Query the Jira MCP for this person's jira_user_id:
  - jira_tickets_closed_per_week: count of tickets resolved/closed by this person this week
  - jira_tickets_in_progress_over_2_sprints: count of tickets assigned to this person that have been "in progress" for more than 2 sprint cycles (absolute threshold from SIGNALS.md - check window length before flagging as absolute threshold)
  - jira_blocked_tickets: count of tickets assigned to this person with a "blocked" or impediment status

  Scope to the project(s) associated with this team, using JQL where supported.

  ### Slack signals (if sources.slack is true):
  PRIVACY CONSTRAINT: Only use public channel data. Do not request or use DM content or private channel content. If the Slack MCP returns DM content, ignore it.
  Query for this person's slack_user_id:
  - slack_public_channel_participation_trend: relative participation trend this week vs. recent history (use a 0–1 normalized score or categorical: up/stable/down - note the unit so baseline comparison is consistent)
  - slack_response_latency_trend: trend in response latency in public channels (up/stable/down or normalized score)

  ### Calendar signals (if sources.calendar is true):
  Query for this person:
  - calendar_meeting_load_pct: percentage of working hours in meetings this week
  - calendar_focus_time_blocks_per_week: count of uninterrupted blocks ≥90 minutes this week

  Record all collected signal values as: { slug, metric_name, current_value, week_queried }
```

**Section: Phase C - Baseline comparison and flag determination**
```
## Phase C - Baseline comparison and flag determination

For each person:
  1. Retrieve their entry from CURRENT_BASELINES.people[slug] (may not exist for new team members).
  2. For each metric with a collected current_value:
     a. Look up the metric's window in CURRENT_BASELINES (may not exist).
     b. Apply BASELINES.md first-run window-thinness check:
        - window does not exist OR len(window) == 0: label "(baseline pending - 0 weeks of data)"; do NOT compute deviation; do NOT flag
        - len(window) == 1 or 2: label "(baseline pending - N weeks of data)"; do NOT flag
        - len(window) >= 3: proceed to deviation computation
     c. If computing deviation:
        - deviation_stddev = (current_value - mean) / stddev
          NOTE: if stddev == 0 and current_value != mean: treat as extreme deviation (>2 stddev); if stddev == 0 and current_value == mean: treat as 0 deviation
        - For lag metrics (github_pr_review_lag_days, calendar_meeting_load_pct): flag on UPWARD deviation (deviation_stddev > 0 is concerning)
        - For all other metrics: flag on DOWNWARD deviation (deviation_stddev < 0 is concerning)
  3. Apply SIGNALS.md two-signal rule (verbatim - do not substitute):
     - RED: any single signal at absolute deviation_stddev >= 2 in the concerning direction
     - YELLOW: 2 or more signals each at deviation_stddev >= 1 in the concerning direction, AND no signal hits RED threshold
     - GREEN: everything else (including all baseline-pending states)
  4. Record per person: { slug, flag_status (GREEN/YELLOW/RED/PENDING), triggering_signals [], non_flagged_signals [] }

CRITICAL: Do NOT update baselines.json during this phase. Baseline updates happen in Phase E, after output is generated. Using this week's value in the baseline before comparing it would corrupt the deviation calculation.
```

**Section: Phase D - Dashboard output**
```
## Phase D - Dashboard output

Render the following dashboard. Follow the format exactly.

### Summary table

## Team Health Pulse - [ISO week from injected date]

Sources active: [list sources where true] | Unavailable: [list sources where false] (or "None unavailable" if all active)

| Name | Status | Signals Checked |
|------|--------|-----------------|
[One row per team member. Status: GREEN / YELLOW / RED / PENDING]
[Signals Checked: "N/11 (source names)" where N is the count of signals queryable with active sources]
[PENDING rows: "(baseline pending - N weeks of data)"]

### Detail sections (flagged individuals only)

For each team member where flag_status is YELLOW or RED:

### [Full Name] - [STATUS] ([yellow: "watch" / red: "immediate attention"])

Signals triggering flag ([N] of [threshold] required):

[For each triggering signal, output exactly this format:]
- **[Human-readable metric name]:** [current_value] [unit] this week vs. 8-week avg [mean] [unit] ([delta:+/-N.N], [stddev_delta:+/-N.N] stddev)
  Source: [GitHub / Jira / Slack / Calendar] MCP

Other signals (within baseline):
[Brief one-line summary: "PRs merged: N (baseline avg: N.N) - within normal range"]

DO NOT render a detail section for GREEN or PENDING team members - table row only.
DO NOT use psychological language (see PRIVACY.md Rules 1 and 4). All observations are behavioral.
DO NOT compare one person to another. Every signal references only that person's own baseline.

### Disclaimer (mandatory - append after all output, verbatim)

---
*These signals are indicators, not diagnoses. Always talk to your people before drawing conclusions.*
```

**Section: Phase E - State writes**
```
## Phase E - State writes

Execute Phase E only after Phase D output is complete. Do not write state before output.

### Step 1: Update baselines.json

For each person × metric (where a current_value was collected this week):
  Apply BASELINES.md Updating a Baseline algorithm exactly:
  1. Append current_value to the window array (create empty array if metric is new)
  2. If len(window) > baseline_window_weeks (from config.json, default 8): remove first element
  3. mean = sum(window) / len(window)
  4. stddev = sqrt(sum((x - mean)^2 for x in window) / len(window))
     Special case: if len(window) == 1, stddev = 0
  5. last_value = current_value
  6. Set computed_to = today's date (from injected date)
  7. If this is the first entry for this person: set computed_from = today's date

Build the ENTIRE updated baselines.json in memory first.
Then write with a SINGLE Write tool call to .team-health/baselines.json.
The file must include schema_version, last_updated (today), and the complete people object.
DO NOT call Write multiple times (once per person) - this risks partial writes.

### Step 2: Write pulse history snapshot

First, run: Bash(mkdir -p .team-health/pulse-history)
Then write to: .team-health/pulse-history/<ISO-week>.md
(ISO week is the value injected at the top: !`date +%Y-W%V` - e.g., 2026-W11)

Contents:
# Team Pulse - [ISO week]

**Run date:** [today YYYY-MM-DD]
**Sources active:** [list sources where true, or "None"]
**Unavailable:** [list sources where false with PRIVACY.md degradation phrasing, or "None"]

## Summary

| Name | Status |
|------|--------|
[one row per team member]

## Flagged Members

[For each YELLOW/RED member: name, status, triggering signals with deltas in the same format as Phase D]

[If no flagged members: "No members flagged this week."]

---
*These signals are indicators, not diagnoses. Always talk to your people before drawing conclusions.*
```
  </action>
  <verify>
    <automated>grep -q "disable-model-invocation: true" /Users/kamallaungani/Developer/engineering-manager-skills/.claude/commands/team-health/pulse.md && grep -q "Pre-flight Check" /Users/kamallaungani/Developer/engineering-manager-skills/.claude/commands/team-health/pulse.md && grep -q "Phase E" /Users/kamallaungani/Developer/engineering-manager-skills/.claude/commands/team-health/pulse.md && grep -q "indicators, not diagnoses" /Users/kamallaungani/Developer/engineering-manager-skills/.claude/commands/team-health/pulse.md && grep -q "two-signal" /Users/kamallaungani/Developer/engineering-manager-skills/.claude/commands/team-health/pulse.md && echo "PASS - front matter, pre-flight, phase E, disclaimer, and two-signal rule all present" || echo "FAIL"</automated>
  </verify>
  <done>
.claude/commands/team-health/pulse.md exists with:
- Front matter: description, argument-hint, allowed-tools (Read, Write, Bash(date +%Y-%m-%d), Bash(date +%Y-W%V), Bash(mkdir -p .team-health/pulse-history)), disable-model-invocation: true
- Date injection lines: Current date and Current ISO week
- Pre-flight Check block (verbatim boilerplate)
- Required Reference Reads section (SIGNALS.md, BASELINES.md, PRIVACY.md in order)
- Phase A: sources map gating, graceful degradation
- Phase B: per-person signal collection loop with all 4 source blocks, privacy constraint on Slack, github_org scoping
- Phase C: baseline comparison, first-run window-thinness check, two-signal flagging rule
- Phase D: dashboard summary table + per-flagged-person detail sections in exact PULSE-06 format + verbatim disclaimer
- Phase E: atomic single-write of baselines.json + pulse-history snapshot with mkdir -p
All 10 smoke test scenarios from docs/testing/phase-3-smoke-tests.md pass when the command is invoked in a live Claude Code session with Phase 1 setup complete and GitHub MCP configured.
  </done>
</task>

</tasks>

<verification>
Structural check (automated):
```
grep -q "disable-model-invocation: true" .claude/commands/team-health/pulse.md && \
grep -q "Pre-flight Check" .claude/commands/team-health/pulse.md && \
grep -q "Phase B" .claude/commands/team-health/pulse.md && \
grep -q "Phase C" .claude/commands/team-health/pulse.md && \
grep -q "Phase D" .claude/commands/team-health/pulse.md && \
grep -q "Phase E" .claude/commands/team-health/pulse.md && \
grep -q "indicators, not diagnoses" .claude/commands/team-health/pulse.md && \
grep -q "baselines.json" .claude/commands/team-health/pulse.md && \
grep -q "pulse-history" .claude/commands/team-health/pulse.md && \
echo "PASS" || echo "FAIL"
```

Functional verification (manual - run all 10 scenarios in docs/testing/phase-3-smoke-tests.md):
- Scenario 1: All configured team members appear in output
- Scenario 2: Personal baseline used, no team comparisons
- Scenario 3: Correct signals per active source, unavailable sources named
- Scenario 4: Two-signal flagging rule - sub-tests A (no flag), B (YELLOW), C (RED)
- Scenario 5: Summary table all members, detail sections for flagged only
- Scenario 6: Flagged signal output has metric name + delta + stddev delta + source
- Scenario 7: Disclaimer verbatim at end of every run
- Scenario 8: baselines.json has provenance fields, Carol initialized as first-run
- Scenario 9: pulse-history/YYYY-WNN.md created on fresh install
- Scenario 10: All 4 unavailable sources named with PRIVACY.md degradation phrasing
</verification>

<success_criteria>
.claude/commands/team-health/pulse.md is a complete, invocable Claude Code slash command. Managers can run it weekly to get a team health dashboard with personal-baseline-relative signal flags. All 10 smoke test scenarios pass. The file follows Phase 1/2 conventions exactly: Pre-flight Check boilerplate, disable-model-invocation: true, date injection, github_org scoping, atomic baselines.json write in Phase E after output generation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-team-pulse/03-01-SUMMARY.md` following the standard summary template.
</output>
