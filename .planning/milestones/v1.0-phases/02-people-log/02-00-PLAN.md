---
phase: 02-people-log
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/testing/phase-2-smoke-tests.md
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
  - LOG-06

must_haves:
  truths:
    - "A manual tester can reproduce every LOG-01 through LOG-06 scenario step-by-step without ambiguity"
    - "Each scenario specifies setup preconditions, exact command invocation, and observable pass/fail criteria"
    - "The commitment dual-write scenario (entries AND open_commitments) is explicitly covered"
  artifacts:
    - path: "docs/testing/phase-2-smoke-tests.md"
      provides: "Authoritative manual test procedure for all 6 people log scenarios"
      contains: "LOG-01, LOG-02, LOG-03, LOG-04, LOG-05, LOG-06"
  key_links:
    - from: "docs/testing/phase-2-smoke-tests.md"
      to: ".claude/commands/team-health/log.md"
      via: "Smoke test scenarios reference the exact command invocations the log command must handle"
---

<objective>
Create the Wave 0 validation contract for Phase 2: a step-by-step manual smoke test document covering all 6 LOG requirements. This document is the verification anchor for Plan 02-01 - the executor of the log command verifies their work against these scenarios.

Purpose: Establish the test contract before writing the command so verification is unambiguous.
Output: docs/testing/phase-2-smoke-tests.md - manual test procedure with 6 scenarios covering LOG-01 through LOG-06.
</objective>

<execution_context>
@/Users/kamallaungani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kamallaungani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/state-schemas.json
@.planning/phases/02-people-log/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write phase-2-smoke-tests.md</name>
  <files>docs/testing/phase-2-smoke-tests.md</files>
  <action>
Create `docs/testing/phase-2-smoke-tests.md` with 6 numbered smoke test scenarios. The file must follow this structure exactly:

**Header block:**
- Phase: 2 - People Log
- Command under test: `/team-health:log`
- Prerequisites section: Phase 1 setup complete, `.team-health/config.json` exists with `setup_complete: true`, at least one team member configured (use "Alice Chen" / slug "alice-chen" and "Bob Smith" / slug "bob-smith" as canonical test fixtures throughout all scenarios)

**Scenario 1 - LOG-01: Append entry creates file and adds entry**
- Precondition: `alice-chen.json` does not exist
- Invocation: `/team-health:log Alice` then type a note (e.g. "gave strong feedback on the auth PR")
- Pass criteria: `.team-health/people/alice-chen.json` exists; `entries` array has exactly 1 entry; entry has `id`, `date` (today's date YYYY-MM-DD), `category`, `content`, `created_at` fields; `schema_version` is `"1"`

**Scenario 2 - LOG-02: All 7 category values written correctly**
- Precondition: `alice-chen.json` exists (from Scenario 1 or freshly created)
- Invocations: 7 separate `/team-health:log alice` calls, one note per category domain (use the keyword signals from the research: feedback-given, feedback-received, career, commitment, concern, win, note)
- Pass criteria: After each note, inspect JSON. Category field matches the expected category string exactly. Valid values: `feedback-given`, `feedback-received`, `career`, `commitment`, `concern`, `win`, `note`

**Scenario 3 - LOG-03: Free-form note is structured with date, category, content**
- Precondition: `alice-chen.json` exists
- Invocation: `/team-health:log alice` → type: "gave great code review feedback on the auth refactor PR - clean API design"
- Pass criteria: Entry in JSON has `date` = today, `category` = `"feedback-given"` (inferred from "gave"), `content` contains the original note text (not paraphrased away); `created_at` is a valid ISO 8601 timestamp

**Scenario 4 - LOG-04: Last 3–5 entries shown as context before prompting**
- Precondition: `alice-chen.json` exists with 6 or more entries
- Invocation: `/team-health:log alice` (no note - just open the log)
- Pass criteria: Claude's response shows exactly 5 recent entries in `[YYYY-MM-DD] · [category] - [content]` format before asking "What would you like to log?"; entries 1 through N-5 (oldest) are NOT shown

**Scenario 5 - LOG-05: Natural language query returns accurate answer**
- Precondition: `alice-chen.json` exists with at least 3 entries including one with `category: "career"` containing the word "promotion"
- Invocation: `/team-health:log alice when did I last discuss promotion?`
- Pass criteria: Claude responds with the date and content of the career entry; does NOT prompt for a new log entry; references data from both `entries` and `career_context` if relevant

**Scenario 6 - LOG-06: First-time log creation is graceful**
- Precondition: `bob-smith.json` does NOT exist (Bob is in config but has never been logged)
- Invocation: `/team-health:log Bob`
- Pass criteria: Claude says "Starting a new log for Bob Smith" (or equivalent); `.team-health/people/bob-smith.json` is created with `schema_version: "1"`, `entries: []`, `open_commitments: []`, `career_context` object; no error is shown; Claude then prompts for a note

**Bonus scenario (commitment dual-write - validates LOG-02 integrity):**
- Invocation: `/team-health:log alice` → type: "I promised to connect Alice with the platform lead by end of month"
- Pass criteria: Entry appears in `entries` array with `category: "commitment"`; the same entry also appears in `open_commitments` array with `status: "open"`

**Footer:**
- Note: All tests are manual - invoke in a live Claude Code session with Phase 1 setup complete
- File path reference: People logs at `.team-health/people/<slug>.json`
- Schema reference: `.planning/phases/01-foundation/state-schemas.json`
  </action>
  <verify>
    <automated>test -f /Users/kamallaungani/Developer/engineering-manager-skills/docs/testing/phase-2-smoke-tests.md && grep -c "LOG-0" /Users/kamallaungani/Developer/engineering-manager-skills/docs/testing/phase-2-smoke-tests.md | grep -q "[6-9]" && echo "PASS" || echo "FAIL - file missing or fewer than 6 LOG references"</automated>
  </verify>
  <done>docs/testing/phase-2-smoke-tests.md exists; contains 6 numbered scenarios each referencing their LOG requirement ID; commitment dual-write bonus scenario present; all pass/fail criteria are observable without interpretation</done>
</task>

</tasks>

<verification>
Run: `test -f docs/testing/phase-2-smoke-tests.md && echo PASS || echo FAIL`
All 6 LOG requirement IDs (LOG-01 through LOG-06) appear in the document.
</verification>

<success_criteria>
docs/testing/phase-2-smoke-tests.md exists with 6 scenarios mapping to LOG-01 through LOG-06. A human with no prior context can follow the steps and determine pass/fail without asking clarifying questions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-people-log/02-00-SUMMARY.md` following the standard summary template.
</output>
