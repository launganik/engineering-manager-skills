---
phase: 02-people-log
plan: "01"
type: execute
wave: 2
depends_on:
  - "02-00"
files_modified:
  - .claude/commands/team-health/log.md
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
  - LOG-06

must_haves:
  truths:
    - "Manager can run /team-health:log <name> and append a structured entry to that person's .team-health/people/<slug>.json"
    - "Opening the log shows the last 3–5 entries before prompting for a new note (or 'no previous entries' on first use)"
    - "Manager can query the log with natural language (e.g. 'when did I last discuss promotion?') and get a specific answer without being prompted for a new entry"
    - "Running the command for a person with no existing log file creates the file gracefully with the correct empty schema"
    - "Commitment entries are written to both the entries array and the open_commitments array"
    - "Running any log command before setup is complete routes to the setup flow instead of erroring"
  artifacts:
    - path: ".claude/commands/team-health/log.md"
      provides: "/team-health:log command - append and query modes for people log"
      contains: "disable-model-invocation: true"
  key_links:
    - from: ".claude/commands/team-health/log.md"
      to: ".team-health/people/<slug>.json"
      via: "Read tool reads existing file; Write tool overwrites with mutated entries array"
    - from: ".claude/commands/team-health/log.md"
      to: ".team-health/config.json"
      via: "Pre-flight Check reads config.json; slug lookup reads config.json team array"
---

<objective>
Write `.claude/commands/team-health/log.md` - the single command file that delivers all 6 LOG requirements. This is Phase 2's primary deliverable. The command handles both append mode (add a structured note) and query mode (answer natural language questions about the log) through argument-based mode detection.

Purpose: Give managers persistent, queryable memory about each direct report. This is the memory layer all future commands (prep, pulse) depend on.
Output: .claude/commands/team-health/log.md - a complete Claude Code slash command file.
</objective>

<execution_context>
@/Users/kamallaungani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kamallaungani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/state-schemas.json
@.planning/phases/02-people-log/02-RESEARCH.md
@.claude/commands/team-health/setup.md
@docs/testing/phase-2-smoke-tests.md

<interfaces>
<!-- Key schemas and contracts the executor needs. Extracted from locked sources. -->
<!-- Use these directly - no codebase exploration needed. -->

From .planning/phases/01-foundation/state-schemas.json (locked):

Person file path: .team-health/people/<slug>.json
Required fields: schema_version, name, slug, entries
Valid categories: feedback-given, feedback-received, career, commitment, concern, win, note

Entry object:
{
  "id": "2026-03-13-001",        // <YYYY-MM-DD>-<NNN> daily sequence, zero-padded
  "date": "2026-03-13",          // injected from !`date +%Y-%m-%d`
  "category": "feedback-given",  // inferred from note text
  "content": "...",              // manager's note, verbatim
  "tags": [],                    // empty array by default
  "created_at": "2026-03-13T14:32:00Z"  // from Bash(date +%Y-%m-%dT%H:%M:%SZ)
}

Commitment object (written to open_commitments in ADDITION to entries when category=commitment):
{
  "id": "2026-03-13-002",
  "date": "2026-03-13",
  "content": "...",
  "status": "open"
}

Empty person file template:
{
  "schema_version": "1",
  "name": "Alice Chen",          // from config.json team[].name (canonical case)
  "slug": "alice-chen",          // from config.json team[].slug
  "created": "2026-03-13",
  "last_updated": "2026-03-13",
  "entries": [],
  "open_commitments": [],
  "career_context": {
    "stated_goals": [],
    "last_promo_discussion": null,
    "notes": ""
  }
}

From .claude/commands/team-health/setup.md (Phase 1 reference implementation):
- Slug derivation rule (verbatim): lowercase all letters, replace spaces with hyphens, remove all special characters
- Pre-flight Check pattern: Read config.json → check setup_complete → stop with message if false
- Allowed-tools front matter syntax: allowed-tools: Read, Write, Bash(date +%Y-%m-%dT%H:%M:%SZ)
- disable-model-invocation: true required on all state-writing commands
- Date injection: Current date: !`date +%Y-%m-%d` as first line of command body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .claude/commands/team-health/log.md</name>
  <files>.claude/commands/team-health/log.md</files>
  <action>
Create `.claude/commands/team-health/log.md` with the complete command implementation. Build it section by section in the order below. Every section header and every piece of logic must appear verbatim as prompt instructions - this file IS the prompt.

**Front matter (copy exactly):**
```
---
description: "Append a note to a direct report's people log, or query their log history. Creates the log on first use."
argument-hint: "<name> [or: <name> when did I last...?]"
allowed-tools: Read, Write, Bash(date +%Y-%m-%dT%H:%M:%SZ)
disable-model-invocation: true
---

Current date: !`date +%Y-%m-%d`
```

**Section: Pre-flight Check**
Copy verbatim from setup.md pattern - this is mandatory boilerplate:
```
## Pre-flight Check

Before doing anything else:
1. Read .team-health/config.json using the Read tool.
2. If the file does not exist OR if setup_complete is not true:
   - Tell the user: "Team Health is not set up yet. Run /team-health:setup to configure your team roster and MCP sources."
   - Stop. Do not proceed with the rest of this command.
3. If setup_complete is true, continue.
```

**Section: Name Resolution**
Instructions for resolving $ARGUMENTS to a config.json slug:
1. Extract the person name from $ARGUMENTS. The name is everything before the first query keyword or question mark - if $ARGUMENTS is just a name, the entire value is the name.
2. Do NOT re-derive the slug from the argument text. Instead: read the `team` array from config.json (already read in Pre-flight Check), find the entry whose `name` field is a case-insensitive prefix match for the typed name. Use the `slug` field from that config entry. This guarantees slug consistency with setup.
3. Fuzzy prefix matching: "alice" matches "Alice Chen"; "alice chen" matches "Alice Chen". Match is case-insensitive. If multiple team members match (e.g. two people named "Alex"), list them and ask the manager to clarify.
4. If no match found: "No team member matching '[typed name]' found. Configured team: [list all names from config]. Did you mean one of these?" - stop.
5. File path: `.team-health/people/<slug>.json`

**Section: Mode Detection**
```
## Mode Detection

After resolving the person name:

If $ARGUMENTS (the text after the person name) contains a question mark,
OR if the text after the name starts with any of: when, what, show, list, find, have, did, has, how many, which

→ QUERY MODE: go to the Query section below.

Otherwise:
→ APPEND MODE: go to the Append section below.

If genuinely ambiguous, default to APPEND MODE.
```

**Section: Query Mode**
```
## Query Mode

Read the full contents of .team-health/people/<slug>.json using the Read tool.

Answer the question using ALL of:
- entries array (full history)
- open_commitments array (may contain commitments not yet in entries, or duplicate entries still open)
- career_context object (stated_goals, last_promo_discussion, notes)

Be specific: include dates, category labels, and quoted content where relevant.
Format the answer as a short, direct response - no headers, no markdown tables unless the answer is a list.

Do NOT prompt for a new entry after answering. Stop after answering the question.
```

**Section: Append Mode - Read or Create File**
```
## Append Mode

### Step 1: Read or create the person file

Attempt to read .team-health/people/<slug>.json using the Read tool.

If the file does not exist (Read returns an error or empty):
  - Say: "Starting a new log for [Full Name from config]."
  - Initialize an in-memory person object using this exact template:
    {
      "schema_version": "1",
      "name": "[full name from config.json team entry]",
      "slug": "[slug]",
      "created": "[today YYYY-MM-DD from injected date]",
      "last_updated": "[today YYYY-MM-DD]",
      "entries": [],
      "open_commitments": [],
      "career_context": {
        "stated_goals": [],
        "last_promo_discussion": null,
        "notes": ""
      }
    }
  - Proceed to Step 2.

If the file exists:
  - Parse it into memory.
  - Proceed to Step 2.
```

**Section: Append Mode - Show Context (LOG-04)**
```
### Step 2: Show recent context

If entries array has 1 or more entries:
  Show the last min(5, total entries count) entries in this format:

  ---
  Recent entries for [Name]:
  [YYYY-MM-DD] · [category] - [content]
  [YYYY-MM-DD] · [category] - [content]
  ...
  ---

  Then ask: "What would you like to log?"

If entries array is empty:
  Say: "No previous entries for [Name]. What would you like to log?"

Wait for the manager's response.
```

**Section: Append Mode - Infer Category (LOG-02, LOG-03)**
```
### Step 3: Infer category from the manager's note

Use these keyword signals to infer the category:

| Keywords / signals in the note | Inferred category |
|-------------------------------|-------------------|
| "gave feedback", "told them", "positive feedback", "constructive feedback", "gave", "reviewed" | feedback-given |
| "they said", "feedback from them", "they told me", "received feedback" | feedback-received |
| "career", "promotion", "promo", "goals", "growth", "next level", "staff", "senior" | career |
| "I promised", "I'll follow up", "I will", "committed to", "going to connect", "will connect", "will introduce" | commitment |
| "worried about", "concern", "seems off", "struggling", "flagging", "burnout" | concern |
| "shipped", "won", "great job", "nailed it", "promoted", "recognition", "shoutout" | win |
| (no strong signal from above) | note |

Inference behavior:
- If category is clearly inferred: use it without asking.
- If ambiguous between two categories: state the inferred category and ask "Does that sound right? (or type the correct category: feedback-given, feedback-received, career, commitment, concern, win, note)"
- If the manager types a category name explicitly: use it.
```

**Section: Append Mode - Build and Write Entry (LOG-01, LOG-03)**
```
### Step 4: Build the entry and write the file

1. Get the current ISO timestamp: call Bash(date +%Y-%m-%dT%H:%M:%SZ)
2. Determine today's date from the injected date at the top of this command (YYYY-MM-DD format)
3. Compute the entry ID:
   - Count existing entries in the entries array whose `date` field equals today's date
   - ID format: <YYYY-MM-DD>-<NNN> where NNN is (count + 1) zero-padded to 3 digits
   - Example: if 0 entries today → "2026-03-13-001"; if 2 entries today → "2026-03-13-003"
4. Build the entry object:
   {
     "id": "<computed id>",
     "date": "<today YYYY-MM-DD>",
     "category": "<inferred or confirmed category>",
     "content": "<manager's note verbatim>",
     "tags": [],
     "created_at": "<ISO timestamp from Bash call>"
   }
5. Append the entry to the entries array.
6. If category is "commitment":
   - Also append to open_commitments array:
     {
       "id": "<same id as the entry>",
       "date": "<today YYYY-MM-DD>",
       "content": "<manager's note verbatim>",
       "status": "open"
     }
7. If category is "career" AND the note contains "promotion" or "promo":
   - Set career_context.last_promo_discussion to today's date (YYYY-MM-DD)
8. Set last_updated to today's date.
9. Write the full updated object to .team-health/people/<slug>.json using the Write tool.
   IMPORTANT: Write the complete file - do not truncate or omit existing entries.
10. Confirm: "Logged [category] entry for [Name]." (one line, no extra detail)
```
  </action>
  <verify>
    <automated>MISSING - no automated runner for markdown command files. Use docs/testing/phase-2-smoke-tests.md (created in Plan 02-00) to verify manually. At minimum, check file structure: grep -c "disable-model-invocation: true" /Users/kamallaungani/Developer/engineering-manager-skills/.claude/commands/team-health/log.md | grep -q "1" && grep -c "Pre-flight Check" /Users/kamallaungani/Developer/engineering-manager-skills/.claude/commands/team-health/log.md | grep -q "1" && echo "PASS - front matter and pre-flight present" || echo "FAIL"</automated>
  </verify>
  <done>
.claude/commands/team-health/log.md exists with:
- Front matter: description, argument-hint, allowed-tools (Read, Write, Bash(date +%Y-%m-%dT%H:%M:%SZ)), disable-model-invocation: true
- Date injection line: Current date: !`date +%Y-%m-%d`
- Pre-flight Check block (verbatim boilerplate)
- Name Resolution section (reads slug from config.json, does NOT re-derive)
- Mode Detection section (query vs. append based on argument text)
- Query Mode section (reads entries + open_commitments + career_context, answers, stops)
- Append Mode sections: Step 1 (read or create), Step 2 (show last 5), Step 3 (infer category), Step 4 (build entry, handle commitment dual-write, write file)
All 6 smoke test scenarios from docs/testing/phase-2-smoke-tests.md pass when the command is invoked in a live Claude Code session with Phase 1 setup complete.
  </done>
</task>

</tasks>

<verification>
Structural check (automated):
```
grep -q "disable-model-invocation: true" .claude/commands/team-health/log.md && \
grep -q "Pre-flight Check" .claude/commands/team-health/log.md && \
grep -q "open_commitments" .claude/commands/team-health/log.md && \
grep -q "Query Mode" .claude/commands/team-health/log.md && \
echo "PASS" || echo "FAIL"
```

Functional verification (manual - run all 6 scenarios in docs/testing/phase-2-smoke-tests.md):
- Scenario 1: Append creates file with correct schema
- Scenario 2: All 7 category values written correctly
- Scenario 3: Free-form note structured with date, category, content
- Scenario 4: Last 3–5 entries shown as context
- Scenario 5: Natural language query returns accurate answer
- Scenario 6: First-time log creation is graceful
- Bonus: Commitment written to both entries and open_commitments
</verification>

<success_criteria>
.claude/commands/team-health/log.md is a complete, invocable Claude Code slash command. Managers can append notes, query history, and create new person logs. All 6 smoke test scenarios pass. The file follows Phase 1 conventions exactly: Pre-flight Check boilerplate, disable-model-invocation: true, date injection, slug lookup from config.json.
</success_criteria>

<output>
After completion, create `.planning/phases/02-people-log/02-01-SUMMARY.md` following the standard summary template.
</output>
