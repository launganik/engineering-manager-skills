---
phase: 04-1-1-prep
plan: "01"
type: execute
wave: 2
depends_on:
  - "04-00"
files_modified:
  - .claude/commands/team-health/prep.md
autonomous: true
requirements:
  - PREP-01
  - PREP-02
  - PREP-03
  - PREP-04
  - PREP-05
  - PREP-06
  - PREP-07
  - PREP-08
  - PREP-09
  - PREP-10

must_haves:
  truths:
    - "Manager can run /team-health:prep <name> and receive a structured prep sheet with 5 sections"
    - "Signal flags show specific metric, delta, and source — no opaque scores or diagnostic language"
    - "Open commitments and career context from people log appear in sections 3 and 5"
    - "Talking points are 3-5 questions framed as behavioral observations, not scripts"
    - "Prep sheet degrades gracefully when MCP sources are unavailable"
    - "prep_run timestamp is written to people log only after output is fully rendered"
    - "Lookback window is dynamic — reads last_1on1_date or prep_run, falls back to 14-day default"
  artifacts:
    - path: ".claude/commands/team-health/prep.md"
      provides: "/team-health:prep slash command"
      min_lines: 150
      contains: "disable-model-invocation: true"
  key_links:
    - from: ".claude/commands/team-health/prep.md"
      to: ".team-health/config.json"
      via: "Pre-flight check and name resolution"
      pattern: "config.json"
    - from: ".claude/commands/team-health/prep.md"
      to: ".team-health/people/<slug>.json"
      via: "Read for log synthesis, Write for prep_run timestamp"
      pattern: "people.*slug.*json"
    - from: ".claude/commands/team-health/prep.md"
      to: ".team-health/baselines.json"
      via: "Read for signal deviation comparison"
      pattern: "baselines.json"
    - from: ".claude/commands/team-health/prep.md"
      to: ".claude/team-health/SIGNALS.md"
      via: "Required Reference Read before signal collection"
      pattern: "SIGNALS.md"
    - from: ".claude/commands/team-health/prep.md"
      to: ".claude/team-health/PRIVACY.md"
      via: "Required Reference Read before output generation"
      pattern: "PRIVACY.md"
---

<objective>
Create the `/team-health:prep <name>` command file — a single-person 1:1 prep sheet synthesizing live MCP signals against baselines and people log data into a 2-minute scannable briefing.

Purpose: This is Phase 4's sole deliverable. It is the first command that combines live MCP signals with qualitative people log data in one view.
Output: `.claude/commands/team-health/prep.md`
</objective>

<execution_context>
@/Users/kamallaungani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kamallaungani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-1-1-prep/04-CONTEXT.md
@.planning/phases/04-1-1-prep/04-RESEARCH.md

@.planning/phases/04-1-1-prep/04-00-SUMMARY.md (Wave 0 smoke tests — verification criteria)
@docs/testing/phase-4-smoke-tests.md (verification contract)

<interfaces>
<!-- Key patterns the executor MUST replicate from existing commands -->

From .claude/commands/team-health/pulse.md:
- Front matter: description, argument-hint, allowed-tools, disable-model-invocation: true
- Date injection: `Current date: !`date +%Y-%m-%d``
- Phase A-E execution structure: Setup -> Data Collection -> Comparison -> Output -> State Write
- Pre-flight check: Read config.json, check setup_complete, stop if not configured
- Required Reference Reads: SIGNALS.md, BASELINES.md, PRIVACY.md in order
- Baseline comparison: read baselines.json, apply deviation formula, two-signal rule
- MCP signal collection: per-source gating from config.json sources map
- Output before state writes: render dashboard THEN update files
- Disclaimer verbatim at end

From .claude/commands/team-health/log.md:
- Name Resolution: extract from $ARGUMENTS, prefix match against config.json team array, use slug from config entry
- People log schema: { schema_version, name, slug, created, last_updated, entries[], open_commitments[], career_context{} }
- Graceful first-use: file not found -> create empty structure

From .claude/team-health/SIGNALS.md:
- 11 signals across 4 sources (GitHub: 4, Jira: 3, Slack: 2, Calendar: 2)
- Two-signal rule: RED = 1 signal >= 2 stddev; YELLOW = 2+ signals >= 1 stddev
- Absolute thresholds: jira_tickets_in_progress_over_2_sprints >= 1, jira_blocked_tickets >= 1, calendar_meeting_load_percent > 60, calendar_1on1_adherence missed, github_pr_review_lag_days > 48h

From .claude/team-health/BASELINES.md:
- Deviation formula: (current_value - mean) / stddev
- Window thinness: len < 3 -> "(baseline pending — N weeks of data)", do not flag
- Direction: low=bad for most metrics, high=bad for lag/latency metrics

From .claude/team-health/PRIVACY.md:
- Rule 1: Signals, not diagnoses
- Rule 2: No DM content
- Rule 3: No team-relative scoring
- Rule 4: No psychological labels
- Required disclaimer: "These signals are indicators, not diagnoses. Always talk to your people before drawing conclusions."
- Graceful degradation pattern: "[Source] signals are unavailable — [MCP name] MCP is not configured. [What data cannot be included]."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /team-health:prep command file</name>
  <files>.claude/commands/team-health/prep.md</files>
  <read_first>
    - .claude/commands/team-health/pulse.md (PRIMARY reference — replicate Phase A-E structure, front matter format, baseline comparison, MCP signal collection, output-before-state pattern, disclaimer)
    - .claude/commands/team-health/log.md (Name Resolution block — copy verbatim; people log schema — read pattern for entries, open_commitments, career_context)
    - .claude/team-health/SIGNALS.md (all 11 signal definitions, thresholds, two-signal rule — prep uses same signals but with dynamic lookback window)
    - .claude/team-health/BASELINES.md (deviation formula, window thinness check, first-run behavior)
    - .claude/team-health/PRIVACY.md (4 language rules, required disclaimer verbatim, graceful degradation phrasing)
    - .planning/phases/04-1-1-prep/04-CONTEXT.md (locked decisions: lookback window logic, talking point sources/tone, carried-forward patterns)
    - .planning/phases/04-1-1-prep/04-RESEARCH.md (Pattern 2: Phase A-E structure, Pattern 3: prep sheet five-section output format, Pattern 4: lookback window language, Pattern 5: talking point generation rules, anti-patterns to avoid, open question answers)
    - docs/testing/phase-4-smoke-tests.md (verification contract — command must pass all 13 scenarios)
  </read_first>
  <action>
Create `.claude/commands/team-health/prep.md` with the following exact structure:

**FRONT MATTER (exact YAML):**
```yaml
---
description: "Generate a 2-minute 1:1 prep sheet for a direct report: live signal snapshot, standing items from people log, and suggested talking points."
argument-hint: "<name>"
allowed-tools: Read, Write, Bash(date +%Y-%m-%d)
disable-model-invocation: true
---
```

**DATE INJECTION (immediately after front matter):**
```
Current date: !`date +%Y-%m-%d`
```

No ISO week injection needed (prep does not write pulse-history files).

**PHASE A — Setup and person resolution:**

1. Pre-flight Check — copy VERBATIM from `pulse.md` Pre-flight Check section (3-step: read config.json, check setup_complete, stop if not configured).

2. Name Resolution — copy VERBATIM from `log.md` Name Resolution section (5-step: extract from $ARGUMENTS, match against config.json team array by case-insensitive prefix, use slug from config entry, handle multi-match and no-match). Remove the Mode Detection section from log.md — prep has no query/append mode distinction.

3. Required Reference Reads — copy VERBATIM from `pulse.md` Required Reference Reads section (read SIGNALS.md, BASELINES.md, PRIVACY.md in order; follow exactly; do not improvise).

4. Read baselines.json: Read `.team-health/baselines.json`. If not found, treat as `{ "schema_version": "1", "last_updated": "", "people": {} }`. Store as CURRENT_BASELINES.

5. Read sources map from config.json (already read in Pre-flight Check). Note unavailable sources using PRIVACY.md Graceful Degradation Language.

**PHASE B — People log read and lookback window derivation:**

1. Read `.team-health/people/<slug>.json` using Read tool.
   - If file does not exist or Read returns error: treat as empty person with `entries: [], open_commitments: [], career_context: { stated_goals: [], last_promo_discussion: null, notes: "" }`. Note: "No people log file found for [Name]. Standing items and context reminders will be unavailable."
   - If file exists: parse into memory as PERSON_DATA.

2. Lookback window derivation (LOCKED decision from CONTEXT.md — implement exactly):
   ```
   If PERSON_DATA.last_1on1_date exists and is valid YYYY-MM-DD:
     lookback_start = PERSON_DATA.last_1on1_date
     lookback_source = "since last 1:1 on [date]"
   Else if PERSON_DATA.prep_run exists and is valid YYYY-MM-DD:
     lookback_start = PERSON_DATA.prep_run
     lookback_source = "since last prep on [date]"
   Else:
     lookback_start = today - 14 days (compute from injected date)
     lookback_source = "14-day default (no prior 1:1 date recorded)"
   ```

3. Extract from people log:
   - `open_commitments`: filter PERSON_DATA.open_commitments for entries where status == "open"
   - `career_context`: read PERSON_DATA.career_context (stated_goals, last_promo_discussion, notes)
   - `log_patterns`: scan PERSON_DATA.entries since lookback_start. Count entries by category. If any category appears 3+ times within the last 6 weeks, flag as a recurring theme.
   - `recent_entries`: last 5 entries since lookback_start (newest first). If more than 5, take most recent 5.

**PHASE C — MCP signal collection (single person, dynamic lookback window):**

CRITICAL: Use `lookback_start` (derived in Phase B) as the date filter for ALL MCP queries — NOT the fixed 7-day or 14-day windows from SIGNALS.md. SIGNALS.md defines WHAT to query, Phase B defines WHEN.

For each available source (skip entire source block if sources.<name> is false in config.json):

GitHub signals (if sources.github is true):
- Scope ALL queries to github_org from config.json
- `github_prs_merged`: count of PRs merged by this person since lookback_start
- `github_pr_review_count`: count of PRs reviewed since lookback_start
- `github_pr_review_lag_days`: average days from PR creation to first review for this person's authored PRs since lookback_start. Also check absolute threshold: any PR open >48h with no review.
- `github_commit_days`: count of distinct calendar days with commits since lookback_start

Use whichever tools are available from GitHub MCP — probe by namespace (any tool containing 'github' or starting with 'mcp_github'). Adapt tool names to what is installed.

Jira signals (if sources.jira is true):
- `jira_tickets_closed`: count closed since lookback_start
- `jira_tickets_in_progress_over_2_sprints`: count (absolute threshold, no lookback needed)
- `jira_blocked_tickets`: count (absolute threshold, no lookback needed)
Scope to project(s) for this team using JQL where supported.

Slack signals (if sources.slack is true):
PRIVACY CONSTRAINT: public channel metadata only. Do NOT request or use DM content.
- `slack_channel_participation`: count of distinct public channels with posts since lookback_start
- `slack_response_latency`: median @-mention response time in public channels since lookback_start

Calendar signals (if sources.calendar is true):
- `calendar_meeting_load_pct`: meeting percentage since lookback_start
- `calendar_1on1_adherence`: was 1:1 held within expected cadence window?

For each collected signal, compare to baseline from CURRENT_BASELINES using BASELINES.md deviation algorithm:
1. Look up person's baseline entry: CURRENT_BASELINES.people[slug].metrics[metric_name]
2. If no entry or window length < 3: label "(baseline pending — N weeks of data)", do NOT flag
3. If window >= 3: compute deviation = (current_value - mean) / stddev
   - If stddev == 0 and current_value != mean: extreme deviation (flag)
   - If stddev == 0 and current_value == mean: deviation = 0 (no flag)
4. Apply direction from SIGNALS.md: low=bad for most, high=bad for lag/latency metrics
5. Check absolute thresholds for applicable signals

Apply SIGNALS.md two-signal rule to determine flag_status:
- RED: any single signal at absolute deviation >= 2 stddev in concerning direction
- YELLOW: 2+ signals each >= 1 stddev in concerning direction, none hits RED
- GREEN: everything else (including all baseline-pending)

CRITICAL: Prep does NOT update baselines.json. Prep is a consumer of baselines, not a producer. Only pulse.md updates baselines.

**PHASE D — Prep sheet output:**

Render the five-section prep sheet. Output ALL of this before any state writes.

```
# 1:1 Prep: [Full Name] — [today YYYY-MM-DD]
*Lookback: [lookback_start] to today ([N days] — [lookback_source])*

---

## 1. Status Snapshot

[1-3 sentences: current signal status.
If signals flagged: name the flags factually. E.g., "2 GitHub signals flagged (YELLOW) since [lookback_start]. 4/11 signals checked (GitHub only)."
If no signals flagged: "No signal anomalies since [lookback_start]. [N] signals checked ([active source names])."
If all baseline-pending: "Insufficient baseline history — raw values shown, no flags applied."
If all sources unavailable: "No MCP sources configured — signal analysis unavailable. Prep based on people log only."]

Available signals: [active sources] | Unavailable: [inactive sources with PRIVACY.md degradation phrasing]

---

## 2. Signal Flags

[If any signals flagged, for each flag use pulse.md detail format:]
- **[Human-readable metric name]:** [current_value] [unit] vs. 8-week avg [mean] [unit] ([delta:+/-N.N], [stddev_delta:+/-N.N] stddev)
  Source: [GitHub / Jira / Slack / Calendar] MCP

[If no flags:]
*No flags. All available signals within personal baseline.*

[If signals unavailable, list each with PRIVACY.md degradation language]

---

## 3. Standing Items from People Log

[If open commitments exist:]
**Open commitments (you owe them):**
- [date] — [content] *(open since [N days])*

[Cap at 5 open commitments. If more than 5: show oldest 3 + newest 2, with "and N others" note.]

[If no open commitments:]
*No open commitments in people log.*

[If no people log file:]
*No people log entries recorded. Use /team-health:log [name] to start logging.*

---

## 4. Suggested Talking Points

[Generate 3-5 talking points. Each is a QUESTION the manager can ask.]
[Label each with source type: (signal), (commitment), (career), (pattern)]
[Priority order: (1) RED flags, (2) overdue commitments >30 days, (3) YELLOW flags, (4) career context overdue >90 days since last promo discussion, (5) log patterns (same concern 3+ times in 6 weeks), (6) wins worth acknowledging]

1. [Question] *(source: [type] — [brief context])*
2. [Question] *(source: [type] — [brief context])*
3. [Question] *(source: [type] — [brief context])*

[COMPLIANCE CHECK: After generating talking points, review each against PRIVACY.md Rules 1 and 4. Rewrite any point containing diagnostic or psychological language. Every point must be a behavioral observation framed as a question.]

[If fewer than 3 data sources have content: generate from available sources. Minimum 3 points even if all come from one source.]

---

## 5. Context Reminders

**Goals:** [stated_goals list, or "None recorded"]
**Last promo discussion:** [date, or "Not recorded"] [if recorded: "(N months ago)"]
**Notes:** [career_context.notes, or "—"]

**Recent log entries:**
[Up to 5 entries since lookback_start, newest first:]
- [YYYY-MM-DD] · [category] — [content]

[If no entries since lookback: "No entries since [lookback_start]."]
[If no people log: "No people log entries recorded."]

---

*These signals are indicators, not diagnoses. Always talk to your people before drawing conclusions.*
```

**PHASE E — State write:**

Execute ONLY after Phase D output is complete.

1. Take PERSON_DATA loaded in Phase B (already in memory).
   - If no person file existed: create the base structure: `{ "schema_version": "1", "name": "[full name from config]", "slug": "[slug]", "created": "[today]", "last_updated": "[today]", "last_1on1_date": "[today]", "prep_run": "[today]", "entries": [], "open_commitments": [], "career_context": { "stated_goals": [], "last_promo_discussion": null, "notes": "" } }`
2. Set: `person.last_1on1_date = [today's date]`
3. Set: `person.prep_run = [today's date]`
4. Set: `person.last_updated = [today's date]`
5. Write the COMPLETE updated person object to `.team-health/people/<slug>.json` using Write tool. Do NOT truncate entries, open_commitments, or career_context.
6. Confirm: "Prep sheet generated for [Name]. Next prep will look back from today."
  </action>
  <verify>
    <automated>grep -c "^## Phase [A-E]" .claude/commands/team-health/prep.md</automated>
  </verify>
  <acceptance_criteria>
    - File .claude/commands/team-health/prep.md exists
    - grep "disable-model-invocation: true" .claude/commands/team-health/prep.md returns a match
    - grep "argument-hint" .claude/commands/team-health/prep.md returns match containing "<name>"
    - grep 'allowed-tools: Read, Write, Bash(date +%Y-%m-%d)' .claude/commands/team-health/prep.md returns a match
    - grep -c "^## Phase" .claude/commands/team-health/prep.md returns 5 (Phases A through E)
    - grep "Pre-flight Check" .claude/commands/team-health/prep.md returns a match
    - grep "Name Resolution" .claude/commands/team-health/prep.md returns a match
    - grep "Required Reference Reads" .claude/commands/team-health/prep.md returns a match
    - grep "SIGNALS.md" .claude/commands/team-health/prep.md returns a match
    - grep "BASELINES.md" .claude/commands/team-health/prep.md returns a match
    - grep "PRIVACY.md" .claude/commands/team-health/prep.md returns a match
    - grep "baselines.json" .claude/commands/team-health/prep.md returns a match
    - grep "last_1on1_date" .claude/commands/team-health/prep.md returns a match
    - grep "prep_run" .claude/commands/team-health/prep.md returns a match
    - grep "14 days" .claude/commands/team-health/prep.md returns a match (14-day default fallback)
    - grep "lookback_start" .claude/commands/team-health/prep.md returns a match
    - grep "1. Status Snapshot" .claude/commands/team-health/prep.md returns a match
    - grep "2. Signal Flags" .claude/commands/team-health/prep.md returns a match
    - grep "3. Standing Items" .claude/commands/team-health/prep.md returns a match
    - grep "4. Suggested Talking Points" .claude/commands/team-health/prep.md returns a match
    - grep "5. Context Reminders" .claude/commands/team-health/prep.md returns a match
    - grep "These signals are indicators, not diagnoses" .claude/commands/team-health/prep.md returns a match (verbatim disclaimer)
    - grep "open_commitments" .claude/commands/team-health/prep.md returns a match
    - grep "career_context" .claude/commands/team-health/prep.md returns a match
    - grep "Talking Points" .claude/commands/team-health/prep.md returns a match
    - grep "two-signal rule" .claude/commands/team-health/prep.md returns a match (or "Two-Signal Rule")
    - grep -i "github_org" .claude/commands/team-health/prep.md returns a match (GitHub queries scoped to org)
    - grep "Write tool" .claude/commands/team-health/prep.md returns a match (in Phase E state write)
    - grep "Do NOT" .claude/commands/team-health/prep.md returns at least 3 matches (anti-patterns: don't re-derive slug, don't update baselines, don't truncate entries)
    - No occurrence of "date +%Y-W%V" in the file (prep does not need ISO week injection)
    - wc -l .claude/commands/team-health/prep.md shows at least 150 lines
  </acceptance_criteria>
  <done>The /team-health:prep command file exists at .claude/commands/team-health/prep.md with: front matter (description, argument-hint, allowed-tools, disable-model-invocation: true), date injection, Phase A (pre-flight check + name resolution + reference reads + baselines read), Phase B (people log read + lookback window derivation from last_1on1_date -> prep_run -> 14-day default), Phase C (MCP signal collection with dynamic lookback window + baseline comparison + two-signal rule), Phase D (five-section prep sheet output with disclaimer), Phase E (write prep_run + last_1on1_date to people log after output). All 10 PREP requirements addressed. Passes all 13 smoke test scenarios.</done>
</task>

</tasks>

<verification>
- `.claude/commands/team-health/prep.md` exists with 5 execution phases
- Front matter matches established command pattern (disable-model-invocation: true)
- Pre-flight check, name resolution, and reference reads match existing commands verbatim
- Lookback window derives from last_1on1_date -> prep_run -> 14-day default (CONTEXT.md locked decision)
- Signal collection uses dynamic lookback window, not fixed 7-day
- Five output sections present: status snapshot, signal flags, standing items, talking points, context reminders
- Talking points are question-framed, labeled by source type
- Disclaimer appears verbatim at end
- Phase E writes prep_run only after Phase D output is complete
- All 13 smoke test scenarios passable
</verification>

<success_criteria>
A manager can run `/team-health:prep Alice` and receive a structured 5-section prep sheet that:
1. Shows signal status against personal baselines with specific metrics and deltas
2. Surfaces open commitments and career context from the people log
3. Generates 3-5 talking points framed as questions
4. Degrades gracefully when MCP sources are unavailable
5. Auto-records prep_run timestamp for next invocation's lookback window
</success_criteria>

<output>
After completion, create `.planning/phases/04-1-1-prep/04-01-SUMMARY.md`
</output>
